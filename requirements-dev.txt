# requirements-dev.txt
# Dependencias exclusivas para desarrollo local y CI.
# Instalar con: pip install -r requirements-dev.txt

# Linting / formatting
ruff>=0.4.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # cliente async para TestClient de FastAPI

# Mocking
fakeredis>=2.21.0      # Redis en memoria para tests (no requiere servidor real)

# Type checking (opcional, útil en local)
mypy>=1.9.0
